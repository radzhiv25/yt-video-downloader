{
  "name": "backend",
  "version": "1.0.0",
  "description": "Python backend for YouTube downloader",
  "private": true,
  "scripts": {
    "dev": "python main.py",
    "start": "python main.py",
    "test": "python -m pytest",
    "lint": "flake8 .",
    "format": "black .",
    "install-deps": "pip install -r requirements.txt",
    "setup-venv": "python -m venv venv && source venv/bin/activate && pip install -r requirements.txt"
  },
  "engines": {
    "python": ">=3.8"
  },
  "keywords": [
    "python",
    "youtube",
    "downloader",
    "backend",
    "api"
  ],
  "author": "Your Name",
  "license": "MIT"
}

